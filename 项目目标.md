

# neican.ai 项目目标重构版

## 一句话目标

**neican.ai 要建设成一个基于 OpenClaw 的 AI 行业知识生产引擎，用于持续发现、判断、沉淀和发布 AI 行业的结构性变化。**

这句话要定死。

不是资讯站。
不是 SEO 站。
不是自动发文系统。
不是旧网站升级项目。

它是一个**知识生产引擎**。

---

## 项目使命

neican.ai 的使命是：

**把分散、嘈杂、快速变化的 AI 行业信息，转化为可追踪、可验证、可复用、可发布的知识资产。**

具体来说，它要完成四件事：

第一，发现 AI 行业的新信息。

第二，判断这条信息是否重要。

第三，把重要信息沉淀为事件、实体、主题、概念、声明和时间线。

第四，把其中适合公开表达的部分，发布为日报、洞察文章、主题页、实体页和知识索引页。

---

## 核心定位

neican.ai 不是一个传统内容网站，而是：

**AI 行业情报索引系统。**

**AI 行业知识资产库。**

**AI 辅助编辑与发布系统。**

**OpenClaw 在媒体知识生产场景下的样板工程。**

这四个定位里，最核心的是第一句：

**AI 行业情报索引系统。**

网站只是它的前端呈现。

---

## 核心对象

这个项目不以“文章”为中心。

它以六类对象为中心：

```text
Source：信息源
Raw Item：原始资讯
Event：事件
Entity：实体
Topic / Concept：主题与概念
Claim：可验证声明
```

文章、日报、专题页、Hugo 页面，都是这些对象的输出结果，不是系统的根。

这个非常关键。

以前的逻辑是：

```text
信息 → 文章
```

现在的逻辑是：

```text
信息 → 事件 → 知识资产 → 内容输出
```

---

## 系统要解决的问题

项目要解决的不是“怎么多发文章”。

而是这几个问题：

第一，AI 行业信息太多，真假混杂，人工筛选成本高。

第二，大量资讯生命周期很短，发完就废，无法沉淀为长期资产。

第三，单篇文章之间缺乏关系，无法形成主题、实体、时间线和知识网络。

第四，AI 自动写作容易制造低价值内容，反而伤害网站权重和可信度。

第五，真正有价值的是行业判断、事实链、结构化索引和长期可复用知识，而不只是快讯。

所以项目的目标不是提高内容产量，而是提高**信息转化为知识资产的效率**。

---

## 系统输出

neican.ai 最终输出五类内容。

第一类是**日报/周报**。

用于承接中低价值但值得记录的信息。

第二类是**洞察文章**。

只处理 A 级重要事件，不追求数量。

第三类是**实体页**。

比如 OpenAI、Anthropic、Google DeepMind、Gemini、Claude、Cursor。

第四类是**主题/概念页**。

比如 AI Agent、Vibe Coding、RAG、MCP、AI Search、Embodied AI。

第五类是**时间线/综合报告**。

用于把短期事件沉淀成长期脉络。

---

## 内容原则

neican.ai 的内容原则是：

**少发垃圾，多沉淀结构。**

不是每条资讯都生成 URL。

不是每个热点都写文章。

不是每篇文章都追求爆款。

每条信息进入系统后，只能有几种命运：

```text
A 级：生成独立洞察文章，并更新知识资产
B 级：进入日报，并更新知识资产
C 级：作为已有事件或声明的补充来源
D 级：忽略或仅记录
```

这就是系统的质量阀门。

这个原则来自前面我们已经讨论过的判断：Google 对大规模生成“没有新增价值”的页面存在明确风险，而 neican.ai 应避免被做成低价值 AI 内容工厂。你之前整理的材料里也明确指出，项目要从“生成文章”升级为“生成事实卡、判断卡、时间线、实体页”，并把网站定位为“AI 行业情报索引 + 人机协作分析实验室”。

---

## 技术目标

技术上，neican.ai 要做成一个 **OpenClaw 原生知识生产工作区**。

它的目标不是堆技术，而是建立一个稳定闭环：

```text
采集
→ 清洗
→ 去重
→ 事件建模
→ 实体/主题归档
→ 编辑决策
→ 知识资产更新
→ 内容生成
→ Hugo 发布
```

第一阶段技术目标很克制：

```text
OpenClaw：智能中枢
Sub-agent：低权限沙箱抓取
SQLite：运行账本
Memory Wiki：知识资产
Hugo：公开页面生成
Git：版本与回滚
Codex：编码施工
```

不把项目目标写成“升级旧 Hugo 网站”。

应该写成：

**建设一个以 OpenClaw 为运行时的 AI 行业知识生产系统，Hugo 只是它的公开发布层。**

---

## OpenClaw 中的角色目标

如果放到 OpenClaw 体系里，neican.ai 应该是一个业务子智能体系统。

建议这样定义：

```text
neican-editor：主业务 Agent
sandbox-fetcher：低权限抓取子 Agent
```

`neican-editor` 负责：

```text
编辑判断
知识资产维护
Codex 协作
内容生成
审核协调
发布控制
```

`sandbox-fetcher` 负责：

```text
RSS 抓取
网页抓取
正文清洗
结构化抽取
隔离外部不可信内容
```

这个设计不是为了复杂，而是为了边界清楚。

外部网页内容不可信，必须先进沙箱。
知识写入和发布动作必须由主 Agent 控制。

---

## 第一阶段 MVP 目标

第一阶段不要写成“完成 neican.ai 全面重构”。

应该写成：

**跑通一个最小知识生产闭环。**

MVP 目标：

```text
1. 创建 neican-editor OpenClaw 工作区
2. 创建 sandbox-fetcher 子智能体
3. 放入项目约束文档
4. 用 Codex 初始化代码骨架
5. 接入少量高质量 AI 信源
6. 支持资讯抓取与去重
7. 支持事件建模
8. 支持 A/B/C/D 内容决策
9. 支持日报 Markdown 生成
10. 支持 A 级文章草稿生成
11. 支持 Memory Wiki 知识资产更新
12. 支持 Hugo 导出和本地 build 校验
```

第一阶段成功标准：

```text
能稳定抓取
能去重
能生成事件
能分级
能生成日报
能生成候选文章
能更新知识资产
能导出 Hugo
能审计运行过程
```

---

## 非目标

这个也要写清楚，否则 Codex 和后续架构会发散。

第一阶段明确不做：

```text
不做传统 CMS
不做 Web Admin
不做复杂知识图谱
不做多 Agent 大组织
不做大规模自动发文
不做用户系统
不做推荐系统
不做旧内容全量迁移
不做全站视觉改版
不做完整 SEO 重构
```

尤其是最后两点很重要。

现在这个项目不是“旧站优化项目”。

它是“新知识生产引擎建设项目”。

旧站内容、旧站页面、旧站 SEO，只在后续作为数据源和发布层适配，不是第一阶段主线。

---

# 最终版项目目标表述

我建议你以后统一用这段：

```text
neican.ai 项目的目标，是建设一个基于 OpenClaw 的 AI 行业知识生产引擎。

它不是传统资讯站，也不是自动发文系统，而是一个将 AI 行业信息转化为结构化知识资产的智能工作区。

系统以事件为核心，把外部资讯经过采集、清洗、去重、事件建模、实体归档、主题归档、声明抽取和编辑决策后，沉淀到 Memory Wiki，并按价值等级输出为日报、洞察文章、实体页、主题页、概念页和时间线。

第一阶段只追求跑通最小闭环：OpenClaw 子智能体工作区、低权限沙箱抓取、SQLite 运行账本、Memory Wiki 知识资产、Hugo 静态发布和 Codex 编码施工。

项目不以内容数量为目标，而以信息判断质量、知识沉淀能力、发布可控性和长期复用价值为目标。
```

这段可以直接放到新的 `PROJECT_BRIEF.md` 顶部。

---

## 更短的版本

如果要一句话写在 README 里：

```text
neican.ai 是一个基于 OpenClaw 的 AI 行业知识生产引擎，用于把分散的 AI 行业资讯转化为事件、实体、主题、声明和可发布的长期知识资产。
```

---

## 给 Codex 的项目目标版本

给 Codex 看，可以更工程化一点：

```text
本项目不是旧网站升级项目，也不是传统 CMS 项目。

本项目要实现一个 OpenClaw 原生的 AI 行业知识生产工作区。

系统输入是 AI 行业资讯；核心处理对象是 Event、Entity、Topic、Concept 和 Claim；输出是 Memory Wiki 知识资产与 Hugo Markdown 页面。

第一阶段目标是跑通：
采集 → 清洗 → 去重 → 事件建模 → 编辑决策 → 知识资产更新 → 日报/文章草稿生成 → Hugo 导出。

不要实现旧站迁移、Web Admin、多 Agent 架构、复杂数据库、知识图谱、全站 SEO 改版。
```

---

## 这次目标重构的关键变化

以前我们容易被这些问题牵着走：

```text
旧站怎么改？
Hugo 是否合适？
n8n 要不要保留？
SEO 怎么优化？
文章怎么排名？
```

现在要换成：

```text
这个知识生产引擎到底生产什么？
它的核心对象是什么？
它怎么判断信息价值？
它怎么沉淀知识资产？
它怎么控制发布质量？
它怎么通过 OpenClaw 运行？
```

这才是主线。

旧系统只是素材库和发布历史。
不是项目目标。
