# Workflow Reference

This Skill workflow is inactive because `neican-editor-dev` is initialized as an OpenClaw sub-agent project.

Use the agent package instead:

```text
openclaw/agent/
```

If this repository later becomes a Skill or combined artifact, update `docs/openclaw-contract.md`, `docs/project_manifest.md`, and this workflow before deploying.
